<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Task71update"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateActivity"></activity>
        <activity android:name=".DatabaseHelper"></activity>
        <activity android:name=".Item"></activity>
        <activity android:name=".ItemAdapter"></activity>
        <activity android:name=".ShowAllItemsActivity"></activity>
        <activity android:name=".ItemDetailActivity"></activity>
        <activity android:name=".ShowAllItemsOnMapActivity"></activity>
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBwR2felRHLH_GVZ2Zez0DI4o1VQzE3UcI"/>
    </application>

</manifest>